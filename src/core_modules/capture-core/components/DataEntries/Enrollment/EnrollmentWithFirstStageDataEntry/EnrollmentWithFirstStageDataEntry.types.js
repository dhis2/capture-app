// @flow
import type { ProgramStage, RenderFoundation } from '../../../../metaData';

export type Props = {
    firstStageMetaData: {
        stage: ProgramStage,
    },
    formFoundation: RenderFoundation,
};
