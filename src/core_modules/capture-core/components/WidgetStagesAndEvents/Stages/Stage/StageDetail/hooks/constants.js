export const DEFAULT_NUMBER_OF_ROW = 5;

export const SORT_DIRECTION = Object.freeze({
    DEFAULT: 'default',
    ASC: 'asc',
    DESC: 'desc',
});

export const MULIT_TEXT_WITH_NO_OPTIONS_SET =
    'could not create the metadata because a MULIT_TEXT without associated option sets was found';
