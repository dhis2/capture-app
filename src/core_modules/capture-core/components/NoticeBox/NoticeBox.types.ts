export type NoticeBoxProps = {
    formId: string;
};

export type HiddenFieldEffect = {
    content: string;
    name: string;
    hadValue: boolean;
};

export type ContentListItem = {
    key: string;
    text: string;
};
