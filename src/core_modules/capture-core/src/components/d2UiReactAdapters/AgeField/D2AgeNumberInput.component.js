// @flow
import * as React from 'react';
import D2TextField from '../TextField/D2TextField.component';

const D2AgeNumberInput = (props: any) => (
    <D2TextField
        type="text"
        {...props}
    />
);

export default D2AgeNumberInput;
